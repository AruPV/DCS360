library(simEd)

#dgamma (or whatever) for super imposed
#rgamma (")
#curve for super imposed
#make relative density freq = FALSE

#plot(NA, NA, x;o, = c(0,5000), ylim - c(0.20), bty = "'")
#pch for different icons,
s


hist(rgamma(n = 100000, shape = 1, scale = 0.9))